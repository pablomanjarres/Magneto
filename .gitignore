# dependencies
node_modules
.pnp
.pnp.js

# builds
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# tool caches
.turbo
.vercel
coverage
assets/brand/.build/

# secrets. Never, not even "just for testing"
.env
.env.*
!.env.example
*.pem
*.key

# dev tooling that has to live at the root. Editors read it from here,
# so it cannot move, but it is not part of the delivery either.
CLAUDE.md
AGENTS.md
GEMINI.md
.cursorrules
.cursor/
.claude/

# session work log: plans, reviews, pending items. Working notes, not a deliverable.
tasks/

# os / editor
.DS_Store
._*
.idea/
.vscode/
*.swp
