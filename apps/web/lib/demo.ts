/**
 * Delivery 1 has no sign-in yet — authentication is HU_NF and lands in a later
 * sprint. Until then every screen reads the one seeded candidate, and this is
 * the single place that knows it, so swapping in a session touches one line.
 */
export const DEMO_PROFILE_ID = "demo-candidate";
